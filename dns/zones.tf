resource "cloudflare_zone" "devou_ps" {
  zone = "devou.ps"
}

resource "cloudflare_zone" "devoups_online" {
  zone = "devoups.online"
}

resource "cloudflare_zone" "lama-corp_space" {
  zone = "lama-corp.space"
}

resource "cloudflare_zone" "marcerisson_space" {
  zone = "marcerisson.space"
}

resource "cloudflare_zone" "risson_me" {
  zone = "risson.me"
}

resource "cloudflare_zone" "risson_rocks" {
  zone = "risson.rocks"
}

resource "cloudflare_zone" "risson_space" {
  zone = "risson.space"
}

resource "cloudflare_zone" "risson_tech" {
  zone = "risson.tech"
}

resource "cloudflare_zone" "thefractal_space" {
  zone = "thefractal.space"
}
